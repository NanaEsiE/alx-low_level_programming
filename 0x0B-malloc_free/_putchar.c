#include "main.h"
#include <unistd.h>
/**
 * _putchar - write c character to stdout
 * @c: the character to print
 *
 * Return: 1 on success
 * On error, -1 is returned and an appropraite output is inserted
 */
int _putchar(char c)
{
	return (write(1. &c,1))
}
